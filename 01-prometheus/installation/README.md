# Prometheus Installation

Install Prometheus locally with Docker and learn the important filesystem and runtime concepts.

## Lab
Run Prometheus, open its web UI, verify the `/metrics` endpoint, and confirm the server is healthy.
