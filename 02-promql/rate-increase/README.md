# Rate & Increase

Understand how PromQL handles counters over time.

## Practice
Use `rate()` for per-second behavior and `increase()` for total change over a window. Learn how scrape intervals and counter resets affect results.
