# PromQL Histograms

Learn histogram buckets, `_bucket`, `_sum`, and `_count` series.

## Practice
Calculate request latency percentiles with `histogram_quantile()` and reason about bucket selection.
