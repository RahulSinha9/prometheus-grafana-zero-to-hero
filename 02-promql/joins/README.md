# PromQL Joins

Learn vector matching and controlled joins with `on`, `ignoring`, `group_left`, and `group_right`.

## Practice
Combine application metrics with metadata labels while avoiding accidental many-to-many matches.
