# Kubernetes Pod Monitoring

Monitor pod health, restarts, resource usage, and application traffic.

## Practice
Build PromQL queries for container restarts, CPU throttling, memory usage, and unavailable replicas.
