# High Latency Lab

Investigate a service whose request latency increases unexpectedly.

## Practice
Use histogram metrics, percentiles, traffic rate, error rate, and dependency signals to narrow the root cause.
